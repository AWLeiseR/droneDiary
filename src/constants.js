const constants = {
    error: {
        INVALID_USER: 'User must have a id, name and email',
        INVALID_LOCATION: 'Location must have a id, name, latitude and longitude',
        INVALID_NOTE: 'Note must have a id, date, startTime and endTime',
    },
}

module.exports = constants
